language: elixir
elixir:
  - 1.7.0
  - 1.6.6
  - 1.6.5
otp_release:
  - 21.0
  - 20.3
  - 20.1
  - 20.0
sudo: false
matrix:
  include:
  - elixir: 1.6.1
    otp_release: 20.0
    env: FORMAT_CHECK="mix format --check-formatted --dry-run"
script:
  - $(echo $FORMAT_CHECK)
  - mix test
